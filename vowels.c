#include <stdio.h>
void main()
{
    char X, a,e,i,o,u;
    printf("enter a charecater ");
    scanf("%c", &X);

    if (X == 'a' || X == 'e' || X == 'i' || X == 'o' || X == 'u')
    {
        printf("vowels");
    }
  
    else{
        printf("conjucthions");
    }
   
}
